# shushu

A Python CLI. Early scaffold — details to come.

## Install (dev)

```bash
uv venv
uv pip install -e ".[dev]"
```

## Run

```bash
uv run shushu --version
```

## Test

```bash
uv run pytest
```
